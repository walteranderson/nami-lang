fn main() {
    result := factorial(10);
    printf("10! equals %d\n", result);
}

fn factorial(current: Int) Int {
    if current == 0 {
        return 1;
    }
    return current * factorial(current - 1);
}
