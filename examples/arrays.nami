fn main() {
    items := [69, 420, 69420];

    printf("with idx\n");
    loop item, idx in items {
        printf("item[%d] = %d\n", idx, item);
    }

    printf("without idx\n");
    loop item in items {
        printf("item = %d\n", item);
    }
}
