fn msg(str: String) {
    printf("%s\n", str);
}

fn add(a: Int, b: Int) Int {
  return a + b;
}

fn main() Int {
    foo: Int;
    printf("foo = %d\n", foo);
    msg("Adding...");
    foo = add(5, 5);
    printf("foo = %d\n", foo);
    return 0;
}
