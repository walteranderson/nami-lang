fn get_message() string {
    return "Hello from another function!";
}

fn add(a: int, b: int) int {
    return a + b;
}

fn main() {
    msg := get_message();
    answer := add(1, 2);
    printf("%s\n", msg);
    printf("1 + 2 = %d\n", answer);
}
