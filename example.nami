fn add(a: int, b: int) int {
  return a + b;
}

fn main() int {
    foo: int;
    printf("foo = %d\n", foo);
    printf("Adding...\n");
    foo = add(5, 5);
    printf("foo = %d\n", foo);
    return 0;
}
